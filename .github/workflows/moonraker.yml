name: "Image: moonraker"

on:
  workflow_dispatch:
  push:
    branches:
      - main
    paths:
      - "moonraker/**"
      - ".github/**"

jobs:
  build:
    uses: ./.github/workflows/build-image-base.yml
    with:
      app: moonraker
